services:
  - type: web
    name: quake-pro-api
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: bash startup.sh
    envVars:
      - key: GROQ_API_KEY
        sync: false
